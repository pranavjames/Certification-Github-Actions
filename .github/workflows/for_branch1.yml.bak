name: Branch1 pipeline

on:
  push:
    branches:
      - branch1
#  workflow_dispatch:  # Uncomment this if you want to allow manual triggering

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: step1 print all files
        run: |
          ls
